<br>============== ログイン中ユーザーの《<%= @tl %>》という名前のリスト内のツイート、デフォルト件数だけ表示 ==================================<br><br>
<% @client.list_timeline(@tl,:include_entities => true).each do |tl| %>
・<%= tl.text %><br>
  <% tl.media.each do |media| %>
    <%= image_tag(media.media_url,:size => "128x128") %>
  <% end %>
<br>
<% end %>